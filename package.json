{
  "name": "howler-sw",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --minify --legal-comments=none --format=esm --outfile=dist/sw.js"
  },
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.19.12",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "firebase": "^10.7.2"
  }
}
